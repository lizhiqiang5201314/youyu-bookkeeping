# BookkeepingApp 项目记录

## 2026-03-19 开发记录

### ✅ 已完成功能

#### 1. 核心功能
- [x] 本地优先 + 云端同步架构 (Dexie.js + Supabase)
- [x] 账本管理（创建、切换、删除）
- [x] 分类管理（收入/支出分类）
- [x] 记账功能（增删改查）
- [x] 统计图表（趋势图、分类饼图）

#### 2. UI 组件
- [x] iOS 风格滚轮式日期选择器（年/月/日三列）
- [x] 数字键盘（计算器风格）
- [x] 分类网格选择
- [x] 首页账单列表（带编辑/删除按钮）

---

### 🔒 安全修复

#### 密钥硬编码问题 ✅
**问题：** Supabase Anon Key 硬编码在代码中
```ts
// 修复前（危险）
const supabaseKey = import.meta.env.VITE_SUPABASE_ANON_KEY || 
  'sb_publishable_iqqg-CoHFg0C8E4Oqz_PwQ_WF_MquAT'
```

**修复后：**
```ts
// 修复后（安全）
const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseKey = import.meta.env.VITE_SUPABASE_ANON_KEY

if (!supabaseUrl || !supabaseKey) {
  throw new Error('Missing Supabase environment variables')
}
```

**新增文件：** `.env.example`
```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

---

### ⚡ 性能优化

#### 1. 添加指数退避重试机制 ✅
```ts
// db.ts
export async function withRetry<T>(
  fn: () => Promise<T>,
  retries = 3,
  baseDelay = 1000
): Promise<T>
```
- 自动重试 3 次
- 退避延迟：1s → 2s → 4s
- 所有云端同步操作使用重试

#### 2. 防抖/节流工具函数 ✅
```ts
export function debounce<T>(fn: T, delay: number)
export function throttle<T>(fn: T, limit: number)
```

#### 3. 持久化优化 ✅
```ts
// 只持久化配置，不存大量数据
partialize: (state) => ({
  selectedDateRange: state.selectedDateRange,
  // transactions 不存储，从 IndexedDB 读取
})
```

#### 4. 安全日期解析 ✅
```ts
// 避免时区问题
export function parseDate(dateStr: string): Date
export function formatDate(date: Date): string
```

---

### 🐛 Bug 修复

| 问题 | 状态 | 解决方案 |
|------|------|----------|
| 竞态条件 | ✅ | `syncWithCloud` 改为 await |
| 日期时区偏移 | ✅ | 手动格式化 `YYYY-MM-DD` |
| 非响应式依赖 | ✅ | `getCategoryStats` 参数传递 categories |
| 重复排序 | ✅ | 只在展示时排序一次 |
| 数字输入不生效 | ✅ | 添加 `originalAmount` 标记 |

---

### 📁 文件结构

```
src/
├── services/
│   ├── db.ts          # IndexedDB + 工具函数
│   ├── supabase.ts    # Supabase 客户端
│   └── storage.ts     # 本地存储封装
├── stores/
│   ├── bookStore.ts   # 账本状态管理
│   ├── transactionStore.ts  # 交易状态管理
│   └── authStore.ts   # 认证状态管理
├── components/
│   ├── QuickAddModal.tsx      # 记账弹窗
│   ├── EditTransactionModal.tsx  # 编辑弹窗
│   └── DatePicker.tsx         # 日期选择器
└── pages/
    ├── HomePage.tsx       # 首页
    └── StatisticsPage.tsx # 统计页
```

---

### 🔧 技术栈

- **框架：** React + TypeScript + Vite
- **状态管理：** Zustand + Persist
- **本地数据库：** Dexie.js (IndexedDB)
- **云端数据库：** Supabase
- **UI 组件：** Tailwind CSS + shadcn/ui
- **图表：** Recharts

---

### ⚠️ 待办事项

- [ ] Supabase RLS 策略确认
- [ ] 交易记录分页加载（大数据量）
- [ ] 离线模式完善
- [ ] 数据导入/导出功能

---

### 📝 使用说明

1. **安装依赖：** `npm install`
2. **配置环境变量：** 复制 `.env.example` 为 `.env` 并填写
3. **启动开发：** `npm run dev`
4. **构建：** `npm run build`
